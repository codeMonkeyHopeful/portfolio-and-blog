import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "About Me",
  description: "Learn more about codeMonkeyHopeful.",
};

const Me = ({}) => {
  return <div>Info about myself</div>;
};

export default Me;
